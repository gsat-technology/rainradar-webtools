import React, { Component } from 'react';
import RadarList from './radar_list.js';

export default class App extends Component {
  render() {
    return (
      <div>
        <RadarList />
      </div>
    );
  }
}
